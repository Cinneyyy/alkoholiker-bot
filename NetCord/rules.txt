rule(meme_channel_vote; {
    if([
        channel(#memes) &
        cooldown(0s)
    ])
    then({
        react(:arrow_up, :arrow_down)

        if(
            user(@shalom)
        )
        then(
            react(:neutral_face)
        )

        break()
    })
})